%h1{:class => "title"} The Bawdy Electorate story so far

%p{:class => "story"}
  - @story.each do |sentence|
    - if sentence[:reported]
      %span{style: 'text-decoration: line-through;'}
        = "#{sentence[:prompt]} "
    - else
      = "#{sentence[:prompt]} "
